snap:
  stage: build
  script:
    - snapcraft clean
    - snapcraft --use-lxd
  artifacts:
    paths:
      - "*.snap"
    expire_in: 1 week

test-snap:
  stage: test
  script:
    - review-tools.snap-review *.snap
  dependencies:
    - snap